import { AllowMinDirective } from './allow-min.directive';

describe('AllowMinDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowMinDirective();
    expect(directive).toBeTruthy();
  });
});
